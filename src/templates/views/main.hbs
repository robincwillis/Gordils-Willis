{{> '../components/loader' }}
{{> 'header' }}

 <div id="page">
    <div id="content">
			{{> 'splash'}}
			{{> 'work'}}
			{{> 'about-blurb'}}
			{{> 'about'}}
			{{> 'services'}}
			{{> 'contact'}}
    </div>
</div>

{{> 'footer' }}
