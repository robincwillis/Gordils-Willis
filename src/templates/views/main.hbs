{{> 'header' }}

 <div id="page">
    <div id="content">
			{{> 'splash'}}
			{{!-- {{> 'intro'}} --}}
			{{> 'work'}}
			{{> 'services'}}
			{{> 'about'}}
			{{> 'contact'}}
    </div>
</div>

{{> 'footer' }}
{{> '../components/loader' }}